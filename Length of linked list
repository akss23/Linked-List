/*
class Node {
  public:
    int data;
    Node *next;

    Node(int x) {
        data = x;
        next = NULL;
    }
};
*/

class Solution {
  public:
    int getCount(Node* head) {
        int cnt=0;
        Node* temp=head;
        while(temp!=NULL){
            // cout<<temp->data;
            temp=temp->next;
            cnt++;
        }
        return cnt;
    }
};
