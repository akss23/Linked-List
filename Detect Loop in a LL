/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    bool hasCycle(ListNode *head) {
        // unordered_map<ListNode* , int> m;
        // ListNode* temp = head;
        // while(temp != NULL){
        //     if(m.find(temp) != m.end()){
        //         return true;
        //     }
        //     m[temp] = 1;
        //     temp = temp -> next;
        // }
        // return false;//this is brute force intuitive method that suggests usage of map data structure
     
     ListNode* slow = head; ListNode* fast = head;
     while(fast!=NULL && fast->next!=NULL){
           slow=slow->next;
           fast=fast->next->next;
           if(slow == fast) return true;
     }
     return false;
    }
};
